sources:
  - name: stripe
    database: raw
    tables:
      - name: payment
        config:
          freshness: 
            warn_after:
              count: 24
              period: hour
            error_after:
              count: 24
              period: hour
          loaded_at_field: _batched_at