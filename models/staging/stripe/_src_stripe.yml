sources:
  - name: stripe
    description: Copy of stripe database
    database: raw
    tables:
      - name: payment
        description: "{{doc('stripe_payments')}}"
        config:
          freshness: 
            warn_after:
              count: 24
              period: hour
            error_after:
              count: 24
              period: hour
          loaded_at_field: _batched_at